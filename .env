# --- Database ---
POSTGRES_USER=ddadjee
POSTGRES_PASSWORD=ddadjee
POSTGRES_DB=ddadjee
POSTGRES_PORT=5432

# --- Redis ---
REDIS_PORT=6379

# --- Kafka ---
KAFKA_ADVERTISED_HOST=localhost
KAFKA_EXTERNAL_PORT=9092

# --- Elasticsearch / Kibana ---
ELASTIC_VERSION=8.14.1
ELASTIC_MEM=1g
ES_PORT=9200
KIBANA_PORT=5601

# Tips:
# 1) Copy to .env and adjust as needed.
# 2) Start base infra:     docker compose up -d postgres redis kafka
# 3) Start tools:          docker compose --profile tools up -d kafdrop
# 4) Start ELK (optional): docker compose --profile elk up -d
